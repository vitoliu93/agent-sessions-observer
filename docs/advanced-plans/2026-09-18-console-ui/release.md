# 控制台 UI 0.6.0 发布

## 大前提

发布完成必须同时满足：main 的远端检查通过、标签发布成功、npm 正式包可下载并运行。

## 小前提

- 版本：`agent-sessions-obs@0.6.0`。
- 用户已明确授权发布、合并与推送。
- 本地冻结依赖安装、类型检查、生产构建通过。
- 发布包预检：仅 7 个文件，包含命令入口与 HTML、CSS、JS；不含测试数据、临时截图或本地配置。
- 功能与页面检查：见 `result.md`。

## 结论

发布进行中，尚未认定 npm 发布成功。
